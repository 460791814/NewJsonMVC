
@{
    ViewBag.Title = "HTML代码转换为JavaScript字符串,HTML转换为js字符串,前端开发转换工具,WEB前端开发";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section header{
    <meta name="keywords"
      content="web前端开发, 前端开发工程师, 前端资源, CSS, JavaScript, Ajax, jQuery, html,html5,css3,浏览器兼容, 前端开发工具, 前端招聘, jQuery API,HTML代码转换为JavaScript字符串, CSS整形和优化工具 , JS压缩工具 , JS格式化工具">
<meta name="description" content="web前端开发，前端开发，关注用户体验，关注国内外最新最好的前端开发技术和前端开发工具">
    <!--[if IE]><link rel="stylesheet" href="/Css/ie.css" type="text/css" media="screen, projection"><![endif]-->
<link rel="stylesheet" href="/Css/screen.css" type="text/css" media="screen, projection">
<link href="/Css/jquery-linedtextarea.css" type="text/css" rel="stylesheet">
       }
 <div class="panel panel-default">
    <div class="panel-heading">
        HTML代码转换为JavaScript字符串
    </div>
    <div class="panel-body">

      
        <textarea id="content" class="json_input" rows="10" style="width: 100%;height: 200px;">
&lt;p style=&quot;text-indent: 2em;&quot;&gt;请在输入框内贴入你需要转换的HTML代码&lt;/p&gt;
&lt;p&gt;HTML转换工具，可以将HTML代码转换为JavaScript字符串&lt;/p&gt;
&lt;p&gt;直接将你所要用程序输出的大串HTML代码贴到输入框中，即可一键生成&lt;/p&gt;
&lt;p&gt;如果您觉得好用，别忘了推荐给朋友！&lt;/p&gt;</textarea>
        <div class="subbtn">
            <input type="button" class="btn btn-primary" onclick="javascript1();" value="转为单引号" class="btn">
            <input type="button" class="btn btn-primary" onclick="javascript();" value="转为双引号" class="btn">
            <label for="arrays-sel"><input type="checkbox" checked="checked" id="arrays-sel" /> 数组拼接</label>
        </div>
        <div style="padding-top: 10px;">
            <textarea id="result" class="json_input" rows="10" style="width: 100%;height: 200px;" placeholder="转换结果"></textarea>
            </div>
        </div>
</div>
<script type="text/javascript">
    function javascript() {
        var isArraySel = $("#arrays-sel").prop("checked");
        var htmlArr = $("#content").val().replace(/\\/g, "\\\\").replace(/\\/g, "\\/").replace(/\'/g, "\\\'").replace(/\"/g, "\\\"").split('\n');
        var len = htmlArr.length;
        var outArr = [];
        if (isArraySel) {
            outArr.push("[");
            jQuery.each(htmlArr, function (index, value) {
                if (value !== "") {
                    if (index === len - 1) {
                        outArr.push("\"" + value + "\"");
                    } else {
                        outArr.push("\"" + value + "\",\n");
                    }
                }

            });
            outArr.push("].join(\"\");");
        } else {
            jQuery.each(htmlArr, function (index, value) {
                if (value !== "") {
                    if (index === len - 1) {
                        outArr.push("\"" + value + "\";");
                    } else {
                        outArr.push("\"" + value + "\"+\n");
                    }
                }
            });
        }
        $("#result").val(outArr.join(""));
    }
    function javascript1() {
        var isArraySel = $("#arrays-sel").prop("checked");
        var htmlArr = $("#content").val().replace(/\\/g, "\\\\").replace(/\\/g, "\\/").replace(/\'/g, "\\\'").split('\n');
        var len = htmlArr.length;
        var outArr = [];
        if (isArraySel) {
            outArr.push("[");
            jQuery.each(htmlArr, function (index, value) {
                if (value !== "") {
                    if (index === len - 1) {
                        outArr.push("\'" + value + "\'");
                    } else {
                        outArr.push("\'" + value + "\',\n");
                    }
                }

            });
            outArr.push("].join(\"\");");
        } else {
            jQuery.each(htmlArr, function (index, value) {
                if (value !== "") {
                    if (index === len - 1) {
                        outArr.push("\'" + value + "\';");
                    } else {
                        outArr.push("\'" + value + "\'+\n");
                    }
                }
            });
        }


        $("#result").val(outArr.join(""));

    }
</script>

@section scripts{

    <script src="/Js/jquery-linedtextarea.js" type="text/javascript"></script>
    
}